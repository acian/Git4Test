<div>
  <div>
  	<p><%= comment.rating %></p>
    <p><%= comment.body %></p>
    <p><%= time_ago_in_words(comment.created_at) %> Ago</p>
    
    <p><%= button_to 'Delete', [comment.post, comment],
                  method: :delete,
                  data: { confirm: 'Are you sure?' } %>
  </p>
  </div>
</div>