
<%= form_for([@post, @post.comments.build]) do |f| %>
  <br>
  <%= f.label :rating %><br>
  <%= f.text_field :rating %> <br>
  <%= f.label :body %><br>
  <%= f.text_area :body %><br>
  <br>
  <%= f.submit %>
<% end %>